# System Patterns

The tutorials are designed as standalone Markdown files, easy to read and understand. Code examples (pseudo-code or Python) are embedded directly within the Markdown to illustrate concepts. The project structure is flat for tutorials in the root, with potential subdirectories for larger, related topics (e.g., `advanced_topics/`).
